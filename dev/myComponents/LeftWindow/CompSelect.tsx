import * as React from 'react';
import * as ReactDOM from 'react-dom';

export const LeftWinSelect = () => {
    switch () {
        case null:
            return <></>;
        default:
            return null;
    }
}